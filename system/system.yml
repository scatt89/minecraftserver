---
- name: Setup timezone
  timezone:
    name: "{{ system_timezone }}"
- name: Creates a cron task to clean the memory cache
  cron:
    name: "clean the memory cache"
    minute: "0"
    hour: "*/1"
    job: "sync; echo 3 > /proc/sys/vm/drop_caches"